var app = angular.module('employeeRecords', []) // creates an AngularJS module and assigns the object to the variable app.
        .constant('API_URL', 'http://localhost:8080/employeemanager/public/');
